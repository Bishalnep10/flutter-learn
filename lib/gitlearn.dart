import 'package:flutter/material.dart';

class Gitlearn extends StatelessWidget {
  const Gitlearn({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("GIT Push Test"),
    );
  }
}
